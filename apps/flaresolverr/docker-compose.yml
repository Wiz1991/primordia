services:
  flaresolverr:
    image: ghcr.io/thephaseless/byparr:latest
    container_name: flaresolverr
    environment:
      - LOG_LEVEL=debug
      - TZ=${TZ}
    healthcheck:
      test: wget --no-verbose --tries=1 --spider http://localhost:8191/health || exit 1
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s
    networks:
      - traefik
    restart: unless-stopped

networks:
  traefik:
    external: true
